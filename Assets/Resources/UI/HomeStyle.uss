/* ======================================== */
/* HomeStyle.uss — HomeScene                */
/* ======================================== */

/* --- Player icon area --- */
.home-icon-border {
    width: 168px;
    height: 168px;
    border-radius: 50%;
    background-color: rgb(209, 209, 209);
    align-items: center;
    justify-content: center;
    align-self: center;
}

.home-icon-mask {
    width: 160px;
    height: 160px;
    border-radius: 50%;
    overflow: hidden;
    align-items: center;
    justify-content: center;
    background-color: white;
    border-width: 0;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.home-icon-mask:active {
    scale: 0.95 0.95;
}

.home-icon-image {
    width: 100%;
    height: 100%;
    -unity-background-scale-mode: scale-to-fit;
}

.home-player-name {
    font-size: 40px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    color: rgb(13, 13, 20);
    margin-top: 15px;
    align-self: center;
}

/* --- Koimikoshi image --- */
.koimikoshi-container {
    width: 1040px;
    height: 498px;
    align-self: center;
    overflow: hidden;
    border-radius: 32px;
    margin-top: 120px;
}

.koimikoshi-image {
    width: 100%;
    height: 100%;
    -unity-background-scale-mode: scale-to-fit;
}

.gacha-desc {
    font-size: 30px;
    -unity-text-align: middle-center;
    color: rgb(26, 26, 33);
    margin-top: 10px;
    align-self: center;
}

/* --- Meet button (pink bordered) --- */
.meet-btn-border {
    width: 708px;
    height: 128px;
    border-radius: 64px;
    align-self: center;
    align-items: center;
    justify-content: center;
}

.meet-btn {
    width: 700px;
    height: 120px;
    border-radius: 60px;
    background-color: white;
    align-self: center;
    align-items: center;
    justify-content: center;
    font-size: 36px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    color: rgb(242, 78, 128);
    border-width: 0;
    transition-property: scale, background-color;
    transition-duration: 0.08s;
    padding: 0;
}

.meet-btn:active {
    scale: 0.95 0.95;
    background-color: rgb(255, 235, 243);
}
