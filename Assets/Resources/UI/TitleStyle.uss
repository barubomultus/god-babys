/* ======================================== */
/* TitleStyle.uss — TitleScene              */
/* ======================================== */

/* --- Logo --- */
.title-logo {
    width: 1040px;
    height: 498px;
    align-self: center;
    -unity-background-scale-mode: scale-to-fit;
}

/* --- Language toggle row --- */
.lang-panel {
    position: absolute;
    bottom: 40px;
    left: 0;
    right: 0;
    flex-direction: row;
    justify-content: center;
    align-items: center;
}

.lang-btn {
    width: 90px;
    height: 36px;
    border-radius: 6px;
    align-items: center;
    justify-content: center;
    margin-left: 7px;
    margin-right: 7px;
    border-width: 0;
    font-size: 20px;
    -unity-font-style: bold;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.lang-btn:active {
    scale: 0.95 0.95;
}

.lang-btn-active {
    background-color: rgb(77, 128, 204);
    color: white;
}

.lang-btn-inactive {
    background-color: rgb(64, 64, 77);
    color: rgb(153, 153, 153);
}

/* --- Save data list panel --- */
.save-panel {
    width: 800px;
    height: 450px;
    background-color: #ffffff;
    border-radius: 36px;
    align-self: center;
    align-items: center;
    padding-top: 10px;
}

.save-panel-title {
    font-size: 30px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    color: white;
    height: 50px;
    width: 100%;
}

.slot-row {
    width: 460px;
    height: 60px;
    margin-top: 5px;
    flex-direction: row;
    align-items: center;
    padding-left: 15px;
    padding-right: 10px;
    border-radius: 4px;
}

.slot-row-exists {
    background-color: rgb(51, 64, 89);
}

.slot-row-empty {
    background-color: rgb(38, 38, 51);
}

.slot-info {
    flex-grow: 1;
    font-size: 20px;
    color: white;
    -unity-text-align: middle-left;
    overflow: hidden;
}

.slot-load-btn {
    width: 70px;
    height: 40px;
    background-color: rgb(77, 153, 102);
    border-radius: 4px;
    border-width: 0;
    font-size: 18px;
    color: white;
    -unity-text-align: middle-center;
    margin-right: 8px;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.slot-load-btn:active {
    scale: 0.95 0.95;
}

.slot-del-btn {
    width: 40px;
    height: 40px;
    background-color: rgb(179, 77, 77);
    border-radius: 4px;
    border-width: 0;
    font-size: 24px;
    -unity-font-style: bold;
    color: white;
    -unity-text-align: middle-center;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.slot-del-btn:active {
    scale: 0.95 0.95;
}

.close-btn-gray {
    width: 150px;
    height: 45px;
    background-color: rgb(128, 128, 128);
    border-radius: 4px;
    border-width: 0;
    font-size: 22px;
    color: white;
    -unity-text-align: middle-center;
    margin-top: 10px;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.close-btn-gray:active {
    scale: 0.95 0.95;
}

/* --- Confirm delete dialog --- */
.confirm-panel {
    width: 350px;
    height: 150px;
    background-color: rgba(38, 26, 26, 0.98);
    border-radius: 8px;
    align-items: center;
    justify-content: center;
    padding: 10px;
}

.confirm-msg {
    font-size: 22px;
    color: white;
    -unity-text-align: middle-center;
    white-space: normal;
    margin-bottom: 15px;
}

.confirm-btn-row {
    flex-direction: row;
    justify-content: center;
}

.confirm-yes {
    width: 90px;
    height: 40px;
    background-color: rgb(179, 77, 77);
    border-radius: 4px;
    border-width: 0;
    font-size: 20px;
    color: white;
    -unity-text-align: middle-center;
    margin-right: 15px;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.confirm-yes:active {
    scale: 0.95 0.95;
}

.confirm-no {
    width: 90px;
    height: 40px;
    background-color: rgb(102, 102, 128);
    border-radius: 4px;
    border-width: 0;
    font-size: 20px;
    color: white;
    -unity-text-align: middle-center;
    margin-left: 15px;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.confirm-no:active {
    scale: 0.95 0.95;
}

/* --- Dev reset button --- */
.dev-reset-btn {
    position: absolute;
    right: 10px;
    width: 180px;
    height: 40px;
    background-color: rgba(77, 77, 77, 0.6);
    border-radius: 4px;
    border-width: 0;
    font-size: 16px;
    color: rgb(255, 102, 102);
    -unity-text-align: middle-center;
    padding: 0;
    transition-property: scale;
    transition-duration: 0.08s;
}

.dev-reset-btn:active {
    scale: 0.95 0.95;
}

/* --- Intro panel --- */
.intro-panel {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0);
    align-items: center;
    justify-content: center;
    transition-property: background-color;
    transition-duration: 0.5s;
}

.intro-panel-visible {
    background-color: rgba(0, 0, 0, 0.9);
}

.intro-text {
    font-size: 36px;
    color: white;
    -unity-text-align: middle-center;
    white-space: normal;
    width: 800px;
    height: 100px;
    opacity: 0;
    translate: -800px 0;
    transition-property: translate, opacity;
    transition-duration: 1.5s;
    transition-timing-function: ease-in-out;
}

.intro-text-visible {
    translate: 0 0;
    opacity: 1;
}

.slot-empty-text {
    font-size: 20px;
    color: rgb(128, 128, 128);
    -unity-text-align: middle-left;
    flex-grow: 1;
    padding-left: 15px;
}
